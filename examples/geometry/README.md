# Geometry example

Demonstrates the variant-based geometry module: vertex arithmetic, 2D and 3D
uniform meshes with periodic wrapping, and element volume computation.

## Build and run

```bash
make run
```
