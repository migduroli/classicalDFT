# Config example

Parses INI and JSON configuration files using `dft::config::parse_config()`
and extracts typed values with `dft::config::get<T>()`.

## Build and run

```bash
make run
```

## Config files

- `config.ini` — INI format
- `config.json` — JSON format
