# Integration example

Demonstrates the GSL-based `Integrator` class with all quadrature methods:
QAGS (adaptive), QNG (non-adaptive), QAGIU (upper semi-infinite),
QAGIL (lower semi-infinite), and QAGI (full infinite).

## Build and run

```bash
make run
```
