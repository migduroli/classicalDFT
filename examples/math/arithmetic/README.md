# Arithmetic example

Compares compensated summation algorithms (Kahan, Neumaier, Klein) against
naive `std::accumulate` on arrays with floating-point rounding issues.

## Build and run

```bash
make run
```
