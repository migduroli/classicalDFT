# Fourier example

Demonstrates the FFTW3 RAII wrappers: `FourierTransform` for forward/backward
FFT with round-trip verification, Parseval's theorem check, and
`FourierConvolution` for delta * constant = constant.

## Build and run

```bash
make run
```
