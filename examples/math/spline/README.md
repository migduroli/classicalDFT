# Spline example

Demonstrates `CubicSpline` (1D interpolation, derivatives, integration) and
`BivariateSpline` (2D surface interpolation with partial derivatives), both
backed by GSL.

## Build and run

```bash
make run
```
