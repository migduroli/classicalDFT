# Console example

Demonstrates the ANSI terminal formatting utilities in `dft::console`:
coloured info/warning/error/debug messages, bold and blink formatting.

## Build and run

```bash
make run
```
